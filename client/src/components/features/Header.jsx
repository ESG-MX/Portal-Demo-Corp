import React, { useState, useRef, useEffect } from 'react';import { useAuth } from '../../hooks/useAuth';import NotificationDropdown from './NotificationDropdown';const Header = ({ isSidebarOpen, setIsSidebarOpen }) => {  const { userProfile } = useAuth();  return (    <header className="h-16 bg-azul-bersa border-b border-naranja-bersa/20 flex items-center justify-between px-8">      <div className="flex items-center gap-4">        <button onClick={() => setIsSidebarOpen(!isSidebarOpen)} className="p-2 text-blanco hover:bg-white/10 rounded-lg">          <svg className="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 6h16M4 12h16M4 18h16" /></svg>        </button>        <span className="text-blanco font-bold text-sm uppercase tracking-tighter">          Oficina: <span className="text-naranja-bersa">{userProfile?.office}</span>        </span>      </div>      <div className="flex items-center gap-4">        <NotificationDropdown />        <div className="text-right hidden sm:block">          <p className="text-sm font-black text-blanco leading-none">{userProfile?.name}</p>          <p className="text-[10px] text-gray-400 font-bold uppercase">{userProfile?.role}</p>        </div>        <div className="w-10 h-10 bg-naranja-bersa rounded-full overflow-hidden flex items-center justify-center text-white font-bold border-2 border-naranja-bersa/50 shadow-lg shrink-0">          {userProfile?.photo ? (            <img src={userProfile.photo} className="w-full h-full object-cover" alt="Profile" />          ) : (            userProfile?.name?.charAt(0)          )}        </div>      </div>    </header>  );};export default Header;