const express = require('express');const router = express.Router();const notificacionesCont = require('../controllers/notificacionesCont');const authModule = require('../middleware/auth');const validateUserWithGraph = authModule.validateUserWithGraph;router.get('/', validateUserWithGraph, notificacionesCont.getNotificaciones);router.get('/unread-count', validateUserWithGraph, notificacionesCont.getNoLeidasCount);router.put('/mark-all-read', validateUserWithGraph, notificacionesCont.marcarTodasLeidas);router.put('/:id/read', validateUserWithGraph, notificacionesCont.marcarLeida);router.post('/', validateUserWithGraph, notificacionesCont.crearNotificacion);module.exports = router;