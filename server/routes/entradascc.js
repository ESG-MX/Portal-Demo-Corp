const express = require('express');const router = express.Router();const { validateUserWithGraph } = require('../middleware/auth');const { rolePermissionMiddleware } = require('../middleware/rolePermissionMiddleware');const entradasccCont = require('../controllers/entradasccCont'); router.use(validateUserWithGraph);router.use(rolePermissionMiddleware);router.get('/search-items', entradasccCont.searchItems);router.get('/search-vendors', entradasccCont.searchVendors);router.get('/warehouses', entradasccCont.getWarehouses);router.post('/create-entry', entradasccCont.createEntry);module.exports = router;