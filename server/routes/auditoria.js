const express = require('express');const router = express.Router();const auditoriaCont = require('../controllers/auditoriaCont');const { validateUserWithGraph, authorize } = require('../middleware/auth');
router.use(validateUserWithGraph);router.get('/logs', authorize('manager'), auditoriaCont.getLogs);router.get('/comparativo', authorize('manager'), auditoriaCont.getComparativo);router.get('/historial', authorize('manager'), auditoriaCont.getHistorial);router.get('/fechas-historial', authorize('manager'), auditoriaCont.getFechasHistorial);router.post('/ejecutar-cierre', authorize('admin'), auditoriaCont.ejecutarCierre);module.exports = router;